import { BordeSeleccionVerdeDirective } from './borde-seleccion-verde.directive';

describe('BordeSeleccionVerdeDirective', () => {
  it('should create an instance', () => {
    const directive = new BordeSeleccionVerdeDirective();
    expect(directive).toBeTruthy();
  });
});
