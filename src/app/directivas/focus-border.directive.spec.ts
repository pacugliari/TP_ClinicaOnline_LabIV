import { FocusBorderDirective } from './focus-border.directive';

describe('FocusBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusBorderDirective();
    expect(directive).toBeTruthy();
  });
});
