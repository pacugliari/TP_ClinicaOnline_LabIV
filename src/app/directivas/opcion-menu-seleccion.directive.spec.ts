import { OpcionMenuSeleccionDirective } from './opcion-menu-seleccion.directive';

describe('OpcionMenuSeleccionDirective', () => {
  it('should create an instance', () => {
    const directive = new OpcionMenuSeleccionDirective();
    expect(directive).toBeTruthy();
  });
});
